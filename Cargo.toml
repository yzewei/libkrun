[workspace]
members = ["src/libkrun", "src/krun_input"]
exclude = ["examples/gtk_display"]
resolver = "2"

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true

[patch.crates-io]
kvm-bindings = { path = "../kvm/kvm-bindings" }
kvm-ioctls = { path = "../kvm/kvm-ioctls" }